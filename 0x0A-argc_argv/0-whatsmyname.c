#include <stdio.h>

/**
 * main - the main function
 * @argc: takes the number of command line arguments passed
 * @argv: array of size argc
 * Return: Always 0 on success
 */

int main(int argc, char *argv[])
{
	if (argc >= 0)
	{
	printf("%s\n", argv[0]);
	}
	return (0);
}
