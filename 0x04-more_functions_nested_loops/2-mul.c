#include "main.h"
/**
 * mul - Multiplication function to multiply two integers
 * @a: Parameter one passed
 * @b: Parameter two passed
 * Return: Always 0 on Success
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
