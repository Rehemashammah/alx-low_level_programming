#include "main.h"
#include <stdio.h>
/**
 * _abs - Entry point for function definition
 * @a: Parameter to be used
 * Return: 0 (success)
 */
int _abs(int a)
{

	if (a < 0)
	{
		return (-a);
	}
	else
	{
		return (a);
	}
}
