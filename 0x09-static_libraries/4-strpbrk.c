#include "main.h"

/**
 * _strpbrk - Function to search for occurence of a string
 * @s: pointer
 * @accept: pointer
 * Return: Pointer
 */
char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
