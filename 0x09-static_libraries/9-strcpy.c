#include "main.h"
#include <string.h>

/**
 * _strcpy - Function to copy the contents of an array
 * @dest: Address to copy to
 * @src: Address to copy from
 * Return: Pointer to destination
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
